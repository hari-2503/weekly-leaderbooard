# Cycling Leaderboard (prototype)

Frontend: static HTML/CSS/JS (frontend/)
Backend: Node.js + Express (backend/) with file-based storage (rides.json).

### How to run locally (optional)
1. Install Node.js (v16+).
2. In `backend/` open terminal and run:
